select
    id,
    match_id,
    index,
    period,
    minute,
    second,
    timestamp,
    type, 
    play_pattern,
    team,
    team_id,
    player,
    player_id,
    position,   
    location, 
    duration,
    under_pressure,
    off_camera,
    out,
    possession,
    possession_team,
    possession_team_id
from {{ source ('raw', 'events') }}